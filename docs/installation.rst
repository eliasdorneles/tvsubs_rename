============
Installation
============

At the command line::

    $ easy_install tvsubs_rename

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tvsubs_rename
    $ pip install tvsubs_rename
